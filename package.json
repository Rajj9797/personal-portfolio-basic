{
  "name": "personal-portfolio-basic",
  "version": "1.0.0",
  "description": "A minimal static portfolio — editable HTML and CSS with GitHub Pages deployment",
  "private": true,
  "scripts": {
    "dev": "live-server --port=3000",
    "serve": "python -m http.server 8080",
    "start": "npm run dev"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
